/**
 * Contains all reducers to modify user data related state
 */

const initialState = {
    username: '',
    email: '',
    isLoading: '',
    error: ''
}

const user = (state = initialState, type) => {
    switch (type) {
    }

    return state
}

export default user
